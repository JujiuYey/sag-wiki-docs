---
title: 模型与集合
description: 聊天模型、Embedding 模型、模型凭据与默认集合配置。
---

# 模型与集合

> TODO：补充模型配置页字段说明、示例 Provider 配置与故障排查表。

## 模型类型

- 聊天模型（用于 RAG 回答生成）。
- Embedding 模型（用于文档切片向量化与查询向量化）。

## 凭据管理

- 通过 `model_config` 表保存 API Base、API Key、模型名等。
- 凭据加密/隔离策略与最小权限原则。

## 集合（Collection）

- Qdrant Collection 与文档库的映射关系。
- 默认集合、隔离集合与共享集合的使用建议。

## 切换与回滚

- 切换默认模型的影响范围（解析任务、检索任务、对话）。
- 切换失败时的回退策略与任务重跑指引。